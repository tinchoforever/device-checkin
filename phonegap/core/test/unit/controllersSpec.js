'use strict';

/* jasmine specs for controllers go here */

describe('controllers', function(){
  beforeEach(module('initApp.controllers'));


  it('should ....', inject(function() {
      expect(3).toBe(3);
  }));

  it('should ....', inject(function() {
    //spec body
  }));
});
